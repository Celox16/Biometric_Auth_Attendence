<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.biometric_auth_attendence">
    <!-- set permission -->
    <!-- connect to server -->
    <uses-permission android:name="android.permission.INTERNET" /> <!-- use biometric -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" /> <!-- use bluetooth -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Biometric_Auth_Attendence"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".UserInfoModify"
            android:exported="true" />
        <activity
            android:name=".AttendanceResult"
            android:exported="true" />
        <activity
            android:name=".AttendanceSearch"
            android:exported="true" />
        <activity
            android:name=".AttendanceCheck"
            android:exported="true" />
        <activity
            android:name=".AddSubject"
            android:exported="true" />
        <activity
            android:name=".ModifySugang"
            android:exported="true" />
        <activity
            android:name=".AttendanceList"
            android:exported="true" />
        <activity
            android:name=".RegisterActivity"
            android:exported="true" />
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true" />
    </application>

</manifest>